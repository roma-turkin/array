#include "../array.h"
#include <stdlib.h>

struct node {
	INDEX index;
	DATA data;
};

ARRAY create_array() {
}

int insert(ARRAY array, INDEX indexIn, DATA dataIn) {
	
}

DATA get(ARRAY array, INDEX index) {

}

int destroy_array(ARRAY array) {

}